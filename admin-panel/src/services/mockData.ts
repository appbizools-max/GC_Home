import { Service, MaidProfile, Booking, Customer } from '../types';

// All runtime data is dynamically fetched from live Supabase tables.
// Mock and seeded dummy arrays have been completely purged.
export const ADMIN_SERVICES_SEED: Service[] = [];
export const ADMIN_MAIDS_SEED: MaidProfile[] = [];
export const ADMIN_BOOKINGS_SEED: Booking[] = [];
export const ADMIN_CUSTOMERS_SEED: Customer[] = [];
